server:
  port: 8080
  secret: change-me-to-random-string

# 视频源: 源名: 路径或URL
# /开头 = 本地目录(自动识别), http开头 = 远程API(自动识别)
sources:
  默认: /videos
  # 舞蹈: /videos/舞蹈
  # 搞笑: /videos/搞笑
  # 远程示例: https://example.com/api/random
