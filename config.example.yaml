server:
  port: 8080
  secret: change-me-to-random-string

# 视频源: 源名: URL
# / 开头 = 本地目录(自动扫描)
# http开头 = 远程API(自动识别)
#
# 远程源格式:
#   JSON: {"url":"...", "data":{"link":"..."}}
#   直接mp4流: 每次请求返回一个随机mp4
#   HTML: 自动提取 video src
sources:
  # --- 本地目录 ---
  # 默认: /videos
  # 舞蹈: /videos/舞蹈
  # 搞笑: /videos/搞笑

  # --- 远程源(已验证) ---
  # 小姐姐视频: JSON API, 返回快手随机视频
  小姐姐: https://tmini.net/api/meinv?mp4=json&r=

  # 绅士视频: 直接mp4流, 每次返回不同快手视频
  绅士视频: https://v.nrzj.vip/video.php

  # 随机视频: 直接mp4流, 快手H.264编码
  随便看: https://api.yujn.cn/api/zzxjj.php

  # tucdn: HTML页面, 内含随机video src
  热舞视频: https://tucdn.wpon.cn/api-girl/index.php?type=video
